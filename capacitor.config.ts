import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.plusailabs.app',
  appName: 'Plus AI Labs',
  webDir: '.firebase/app-plusailabs-com/hosting'
};

export default config;
