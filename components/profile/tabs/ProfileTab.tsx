"use client";

export function ProfileTab() {
  // Buttons moved to ProfileMetrics component
  // This tab is now minimal - just a placeholder for future quick actions
  return (
    <div className="p-4 max-w-4xl mx-auto">
      <div className="text-center text-sm text-gray-500 dark:text-gray-400 py-8">
        Profile settings and quick actions are in the metrics panel →
      </div>
    </div>
  );
}
